#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin>> t;
    while(t--) {
        int n, x, y;
        cin>> n >> x >> y;
        int array[n];
        for(int i = 0; i < n; i++) {
            cin >> array[i];
        }
        
    }

    return 0;
}